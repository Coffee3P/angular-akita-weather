import { ID } from '@datorama/akita';
import { WeatherObject } from 'src/app/weather';

/**
 * A factory function that creates Weather
 */
export function createWeather(params: Partial<WeatherObject>) {
  return {

  } as WeatherObject;
}
